const nextConfig = {
  output: "export",
}
  reactStrictMode: true,
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/mocho271828.github.io',
};

export default nextConfig
export default nextConfig;
